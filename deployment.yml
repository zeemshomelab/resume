apiVersion: apps/v1
kind: Deployment
metadata:
  name: minyx-resume:48
spec:
  replicas: 4
  selector:
    matchLabels:
      app: minyx-resume:48
  template:
    metadata:
      labels:
        app: minyx-resume:48
    spec:
    
      containers:
      - name: minyx-resume:48
        image: zeemlinux/minyx-resume:48
        imagePullPolicy: "Always"
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
