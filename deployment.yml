apiVersion: apps/v1
kind: Deployment
metadata:
  name: minyx-resume:58
spec:
  replicas: 4
  selector:
    matchLabels:
      app: minyx-resume:58
  template:
    metadata:
      labels:
        app: minyx-resume:58
    spec:
    
      containers:
      - name: minyx-resume:58
        image: zeemlinux/minyx-resume:58
        imagePullPolicy: "Always"
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
