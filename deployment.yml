apiVersion: apps/v1
kind: Deployment
metadata:
  name: minyx-resume:38
spec:
  replicas: 3
  selector:
    matchLabels:
      app: minyx-resume:38
  template:
    metadata:
      labels:
        app: minyx-resume:38
    spec:
      containers:
      - name: minyx-resume:38
        image: zeemlinux/minyx-resume:38
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 5000